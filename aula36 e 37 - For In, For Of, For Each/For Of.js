//indice da string
//            01234...
const nome = 'Yuri Andrade';

//obs: quando é feito com string, ele retorna todos os VALORES, de cada iteração do array de string
for (let valor of nome) {
    console.log(valor);
}

//-------------------------------------------------------------------------------------------------

//COM ARRAY

const nomes = [
    'Yuri', 
    'Creuza',
    'Yoman',
    'Enzo'
];

//obs: quando é feito com array, ele retorna todos os VALORES, de cada iteração do array
for (let valor of nomes) {
    console.log(valor);
}

//NÃO FUNCIONA COM OBJETO

//-------------------------------------------------------------------------------------------------

