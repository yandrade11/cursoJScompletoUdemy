// OBJECT KEYS

const produto = { nome: "Caneca", preco: 1.8 };

//Object.keys() é usado para pegar as chaves (nome da propriedade) dos objetos
console.log(Object.keys(produto)); //retorna array somente com as chaves
